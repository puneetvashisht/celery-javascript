import { SearchMessagesPipe } from './search-messages.pipe';

describe('SearchMessagesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMessagesPipe();
    expect(pipe).toBeTruthy();
  });
});
