Purpose:
The Factory Pattern provides a way to create objects without specifying their exact classes. It promotes loose coupling and supports scalability.

Key Features:
Centralizes object creation logic.
Simplifies adding new types without modifying existing code.